import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    root: {
        fontSize: 50,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
    },
    test: {
        fontSize: 100,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
    }
}));


